<h1> Your Awesome Cart</h1>

<% @carted_products.each do |carted_product| %>
<h3> <%=carted_product.name %> - <%= carted_product.quantity %> x <%= number_to_currency(carted_product.product.price) %> : <%= link_to 'Remove', "/carted_products/#{carted_product.id}", method: :delete %></h3>
  <%end%>

  <%= link_to 'Buy Now!', '/orders', method: :post, class: "btn btn-default"%>